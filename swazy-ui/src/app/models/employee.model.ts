// Define a basic Employee interface for now.
// If the backend provides a DTO, this should be updated.
export interface Employee {
  id: string;
  name: string;
  // Add other relevant employee properties here
}
